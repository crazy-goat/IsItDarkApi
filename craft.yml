php-version: 8.4
extensions: "pcntl,json,xml,yaml,curl,openssl,ctype,filter,mbstring,posix,session,sockets,tokenizer,phar,opentelemetry"
shared-extensions: []
sapi: micro
build-options:
  with-upx-pack: false
download-options:
  prefer-pre-built: true
